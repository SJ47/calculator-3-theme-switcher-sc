describe("theme switcher", () => {
  it("should switch theme", () => {
    cy.visit("/")
    // cy.get('[data-cy="background-color"]').should("have.css", "background-color", "rgb(58, 71, 100)")
    // cy.get('[data-cy="theme-input"]').click()
    // cy.get('[data-cy="background-color"]').should("have.css", "background-color", "rgb(230, 230, 230)")
    // cy.get('[data-cy="theme-input"]').click()
    // cy.get('[data-cy="background-color"]').should("have.css", "background-color", "rgb(22, 6, 40)")
  })
})