import React from "react";
import { StyledKeypadContainer } from "./Keypad.styled";

const Keypad = () => {
    return <StyledKeypadContainer>Keypad</StyledKeypadContainer>;
};

export default Keypad;
